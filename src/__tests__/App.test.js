// import React from 'react';
// import App from '../App';

// let wrapped;

// beforeEach(() => {
//   wrapped = shallow(<App />);
// });

// it('shows pictures list', () => {
//   expect(wrapped.find(PictureList).length).toEqual(1);
// });
